// we can use require to use imports

// const addFunc = require('./expo');

// console.log("this is the add func", addFunc(2, 4))

// this is how you get multiple exports 
// you can always have a export and use functions with dot notation
// prefered way is destructuring  -- we should use same nsames as export names if not explicitely defined the name in exports

// const {add, subFun} = require('./expo');

// console.log("this is the add func", add(7, 4), subFun(7, 4))
